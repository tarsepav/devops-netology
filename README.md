# devops-netology"add test line into readme.md" 

# **/.terraform/* - проигнорировать все файлы в подкаталоге, каталог может быть любой вложенности

#исключить все файлы с расширением *.tfstate с любым включением*.tfstate.*

#исключить crash.log

#исключить все *.tfvars файлы

# исключить файлы override.tf, override.tf.json, все включения *_override.tf, *_override.tf.json

#включить !example_override.tf

# Игнорировать CLI конфиг файлы .terraformrc и terraform.rc
